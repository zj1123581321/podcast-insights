# secrets
.env
**/.token_state.json

# 大体积 / 可再生数据（从 view_urls 可重新下载）
data/*/transcripts/
data/*/episodes/episodes_raw.json
data/*/state/

# python
__pycache__/
*.pyc
.venv/
venv/
.python-version

# node
node_modules/

# os
.DS_Store

# 转录服务 view 链接（含 view token，不入库；可由步骤2再生）
data/*/episodes/view_urls.json
data/*/episodes/view_urls.txt
.gstack/

# 发布构建产物（CF Pages 构建时再生）
dist/

# wrangler 本地构建缓存
.wrangler/

# 校对中间产物(每集输入 / 原始修正)
data/*/proofread/
data/*/corrections_raw.json
